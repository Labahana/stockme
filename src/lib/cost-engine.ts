/**
 * Spec cost-engine — bundle/kit costs attributed to components only
 * (Stocky bug #8). Implementation lives in sync/bundle-components + reports.
 */
export {
  runBundleComponentSync,
  type BundleSyncStats,
} from "@/lib/sync/bundle-components";
